 function tabuadaPersonalizada (){
     let num = Number(prompt("digita um numero para obter a tabuada: "));
     for(i = 1; i <= 10; i++){
         a = num * i;
         console.log(num + " x " + i + " = " + a);
     }
 }
 tabuadaPersonalizada (); 
 tabuadaPersonalizada (); 
 tabuadaPersonalizada (); 
 tabuadaPersonalizada (); 
