function verificadorAlfabeto (){
    let letra = prompt("digite uma letra: "); 
    if (letra === "a" || letra === "e" || letra === "i" || letra === "o" || letra === "u"){
        console.log("É uma vogal!");
    }else {
        console.log("É uma consoante!");
    }
}
verificadorAlfabeto ();
